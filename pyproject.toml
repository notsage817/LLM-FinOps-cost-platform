[project]
name = "llm-finops-cost-platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "confluent-kafka>=2.3.0",
    "apache-beam[gcp]>=2.56.0",
    "pyarrow>=14.0.0",
]
